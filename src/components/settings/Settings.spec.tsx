import {render} from '@testing-library/react';
import { Settings } from './Settings';

describe("Settings", () => {
  it("changing settings triggers update", () => {
  
  })

  it("changing name does not trigger update", () => {
  
  })

})