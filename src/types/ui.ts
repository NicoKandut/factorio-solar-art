export interface Config {
  scale: number;
  threshold: number;
  transparency: boolean;
  roboports: boolean;
  tiles: boolean;
  walls: boolean;
  radars: boolean;
  mods: {
    spaceExploration: boolean;
  };
}
